
Porting this variant for Godot because I wanted something that was a single file.
Not an ideal port because the variables need a lot more tweaking to be accurate, and even then I don't know if the physics implementation would be equivalent regardless.

Missing some of the UI FPS/speed counter from original
